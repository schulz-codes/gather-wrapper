{
  "name": "gather-wrapper",
  "version": "0.3.0",
  "description": "Gather desktop wrapper with Teams camera auto-status sync",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsc --watch & sleep 2 && electron .",
    "build": "tsc",
    "build:native": "swiftc -O -o native/camera-check native/camera-check.swift",
    "dist": "pnpm build:native && pnpm build && electron-builder --mac --universal && codesign --force --deep --sign - --entitlements entitlements.mac.plist release/mac-universal/Gather.app && rm -f release/Gather-*.zip && cd release/mac-universal && zip -ry ../Gather-$(node -p \"require('../../package.json').version\")-universal-mac.zip Gather.app",
    "typecheck": "tsc --noEmit",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "echo \"Release v$(node -p \"require('./package.json').version\")\""
  },
  "build": {
    "appId": "codes.schulz.gather-wrapper",
    "productName": "Gather",
    "files": [
      "dist/**/*",
      "native/camera-check",
      "assets/**/*"
    ],
    "asarUnpack": [
      "native/camera-check"
    ],
    "mac": {
      "target": [
        {
          "target": "dmg",
          "arch": [
            "universal"
          ]
        },
        {
          "target": "zip",
          "arch": [
            "universal"
          ]
        }
      ],
      "category": "public.app-category.productivity",
      "identity": null
    },
    "dmg": {
      "title": "Gather"
    },
    "directories": {
      "output": "release"
    }
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron"
    ]
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.0",
    "@changesets/cli": "^2.27.0",
    "@types/node": "^20.0.0",
    "electron": "^41.1.0",
    "electron-builder": "^24.0.0",
    "typescript": "^5.0.0"
  }
}
